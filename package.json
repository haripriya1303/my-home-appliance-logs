{
  "name": "home-appliance-tracker",
  "version": "1.0.0",
  "description": "Home Appliance Tracker - Monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=home-appliance-backend\" \"npm run dev --workspace=home-appliance-frontend\"",
    "dev:backend": "npm run dev --workspace=home-appliance-backend",
    "dev:frontend": "npm run dev --workspace=home-appliance-frontend",
    "build": "npm run build --workspaces",
    "build:backend": "npm run build --workspace=home-appliance-backend",
    "build:frontend": "npm run build --workspace=home-appliance-frontend",
    "start": "npm run start --workspace=home-appliance-backend",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "db:generate": "npm run db:generate --workspace=home-appliance-backend",
    "db:migrate": "npm run db:migrate --workspace=home-appliance-backend",
    "db:seed": "npm run db:seed --workspace=home-appliance-backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}